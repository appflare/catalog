# Cut

Cut is a tiny self-hosted URL shortener that stores links in Workers KV and can lock a link behind a password, give it an expiry, or cap its clicks.
Sign in at `/admin` with the generated admin password to manage links.
