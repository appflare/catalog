# mail2telegram (sandbox build)

This entry exists only to exercise the sandbox build path of the paid tiers, and
will be removed. It installs the same pinned commit of
[TBXark/mail2telegram](https://github.com/TBXark/mail2telegram) as the
`mail2telegram` entry, but the app is built in your own account by the sandbox
Worker instead of by CI, so it needs Workers Paid and sandbox builds enabled.

For what the app does and what the install creates, see the `mail2telegram` entry.
